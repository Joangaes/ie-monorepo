# This file is intentionally minimal for single Docker container deployment
# All routing is handled by nginx inside the container on port 80
# EB will proxy all traffic to the docker container upstream
